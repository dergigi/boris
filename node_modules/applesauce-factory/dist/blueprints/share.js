import { kinds } from "nostr-tools";
import { blueprint } from "../event-factory.js";
import { setMetaTags } from "../operations/common.js";
import { setShareTags, embedSharedEvent, setShareKind } from "../operations/share.js";
import { setZapSplit } from "../operations/zap-split.js";
/** Blueprint for a NIP-18 repost event */
export function ShareBlueprint(event, options) {
    return blueprint(kinds.Repost, setShareKind(event), embedSharedEvent(event), setShareTags(event), setMetaTags(options), setZapSplit(options));
}
