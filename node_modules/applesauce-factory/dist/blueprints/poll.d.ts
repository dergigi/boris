import { PollType } from "applesauce-core/helpers";
import { NostrEvent } from "nostr-tools";
import { EventPointer } from "nostr-tools/nip19";
import { MetaTagOptions } from "../operations/common.js";
import { ZapOptions } from "../operations/zap-split.js";
export interface PollOption {
    id: string;
    label: string;
}
export type PollBlueprintOptions = MetaTagOptions & ZapOptions & {
    /** Poll type - singlechoice or multiplechoice. Defaults to singlechoice */
    pollType?: PollType;
    /** Unix timestamp when the poll ends */
    endsAt?: number;
    /** Relay URLs where responses should be published */
    relays?: string[];
};
/**
 * NIP-88 Poll event (kind 1068) blueprint
 * Creates a poll event with question and options
 */
export declare function PollBlueprint(question: string, options: PollOption[], opts?: PollBlueprintOptions): import("../types.js").EventBlueprint;
export type PollResponseBlueprintOptions = MetaTagOptions & {
    comment?: string;
};
/**
 * NIP-88 Poll Response event (kind 1018) blueprint
 * Creates a response to a poll event
 */
export declare function PollResponseBlueprint(poll: NostrEvent | EventPointer | string, optionIds: string[], opts?: PollResponseBlueprintOptions): import("../types.js").EventBlueprint;
/**
 * Convenience blueprint for single-choice poll responses
 * Creates a response to a poll event with a single option
 */
export declare function SingleChoicePollResponseBlueprint(poll: NostrEvent | EventPointer | string, optionId: string, opts?: PollResponseBlueprintOptions): import("../types.js").EventBlueprint;
