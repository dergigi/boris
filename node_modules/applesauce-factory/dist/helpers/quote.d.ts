import { EventPointer } from "nostr-tools/nip19";
/** Creates a "q" tag for an EventPointer */
export declare function createQTagFromEventPointer(pointer: EventPointer): string[];
/** Adds or merges an EventPointer into a tags list */
export declare function ensureQuoteEventPointerTag(tags: string[][], pointer: EventPointer): string[][];
