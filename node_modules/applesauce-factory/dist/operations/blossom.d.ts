import { EventOperation } from "../types.js";
/** Adds a server to a 10063 event */
export declare function addServer(url: string | URL, replace?: boolean): EventOperation;
/** Removes server matching the url from a 10063 event */
export declare function removeServer(url: string | URL): EventOperation;
