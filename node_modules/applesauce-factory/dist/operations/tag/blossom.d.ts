import { TagOperation } from "../../types.js";
/** Adds a server tag to a 10063 event */
export declare function addBlossomServerTag(url: string | URL, replace?: boolean): TagOperation;
/** Removes all server tags matching the url from a 10063 event */
export declare function removeBlossomServerTag(url: string | URL): TagOperation;
