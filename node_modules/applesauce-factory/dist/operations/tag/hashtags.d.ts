import { TagOperation } from "../../types.js";
/** Adds a hashtag to the live event */
export declare function addHashtag(hashtag: string): TagOperation;
/** Removes a hashtag from the live event */
export declare function removeHashtag(hashtag: string): TagOperation;
